{
  "name": "@secubot/admin-dashboard",
  "private": true,
  "version": "0.0.0",
  "description": "Internal SOC console (Next.js) — see README.md",
  "scripts": {
    "dev": "echo \"Bootstrap Next.js in this directory (see README).\" && exit 1",
    "build": "echo \"Not generated yet\" && exit 1"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
