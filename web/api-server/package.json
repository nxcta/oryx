{
  "name": "@secubot/api-server",
  "private": true,
  "version": "0.0.0",
  "description": "SecuBot control plane API (NestJS) — see README.md",
  "type": "module",
  "scripts": {
    "start:dev": "echo \"Generate NestJS app in this directory (nest new) and wire scripts.\" && exit 1",
    "build": "echo \"Not generated yet\" && exit 1"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
